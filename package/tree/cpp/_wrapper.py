import tree.cpp._base

def run(fn: str, iter: int, verbose: bool=False):
    """_summary_

    Args:
        fn (str): _description_
        iter (int): _description_
        verbose (bool, optional): _description_. Defaults to False.
    """
    tree.cpp._base.run(fn, iter,verbose)
